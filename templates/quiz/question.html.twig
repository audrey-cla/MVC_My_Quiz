{% extends "base.html.twig" %}


  {% block body %}

    <div class="row bg-warning">
        <div class="col-12">{{ question }}</div>
    </div>
<div class="row bg-success">
       {{ form(form) }}
    </div>

{% endblock %}